# html : quais elementos estão sendo usados?
html,h1,ele define o titule ser mais grande e o tamalho
h2,h3,link, Isso significa que o  "link" o CSS para que ele possa utilizar a pagina web aplicando cores.
label:
# css: quais propriedades estão sendo usadas?
background-color,ele coloca a cor do fundo do elemento.
text-shadow,ele defini a sombra do titulo cor .
 display: inline-block;ele combina as caracteristicas dos elementos
 ele permitara que o elemento seja exibido em linha de outros,
 border-radios:De um elemento, Ela pode ser usada para arredondar todos os cantos de uma vez.
 box-shadow:  Ele gera um  adicionamento e efeitos como sombra a elementos HTML
 padding:efine o espaçamento interno com elementos ou seja uma distância entre o seu conteúdo de elemento e tegs e  bordas.
 height:ela defina a altura do elemento que esta programando a área ele conclui margems bordas.
 transform:ele gera trasformações geometricas como mover e giras os elementos
# js: há algum recurso javascript
let, é a palavra-chave e usada para declarar variáveis com escopo de bloco,const), operações assíncronas (async/await), manipula os dados (filter, map, reduce) e ferramentas que podem desenvolver webarrow functions;funções de flecha e funções concisas.
